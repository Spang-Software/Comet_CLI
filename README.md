# Comet\_CLI

Required:
GCC compiler installed on computer
MacOS

Useage:
1. Download CLI and place in your directory of choice
2. Run "make" comand in the same directory to build program
3. Include the programs location in $PATH variable (e.g., export PATH="$PATH:/Users/username/comet\_cli/bin")
